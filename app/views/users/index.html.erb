<% provide :title, t(".index_title") %>
<h1>
  <%= t ".index_title" %>
</h1>
<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, user %>
    </li>
  <% end %>
  <%= will_paginate @users %>
</ul>
