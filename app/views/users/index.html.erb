<% provide :title, t("users") %>
<h1>
  <%= t "users" %>
</h1>
<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, user %>
    </li>
  <% end %>
  <%= will_paginate @users %>
</ul>
