<%= link_to favorites_path(user_id: current_user.id, book_id: @book.id),
  method: :post, remote: true do %>
  <span class="glyphicon glyphicon-heart-empty favorite" aria-hidden="true"></span>
<% end %>
