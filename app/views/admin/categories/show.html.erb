<p id="notice"><%= notice %></p>

<p>
  <strong><%= t "title.name" %>:</strong>
  <%= @category.name %>
</p>

<p>
  <strong><%= t "title.email" %>:</strong>
  <%= @category.content %>
</p>

<%= link_to t("features.edit"), edit_admin_category_path(@category) %> |
<%= link_to t("features.back"), admin_categories_path %>
