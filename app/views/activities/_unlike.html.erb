<%= form_for [object, object.likes.find_by(user_id: current_user.id)],
  html: {method: :delete, class: "form-inline"}, remote: true  do |f| %>
  <%= f.submit t(".unlike"), class: "submitLink",
    title: t(".unlike_message") %>
<% end %>
