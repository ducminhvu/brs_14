<div class="col-md-12">
  <%= form_for [review, @comment], remote: true do |f| %>
    <%= f.hidden_field :review_id, value: review.id %>
    <%= f.text_field :content, placeholder: t("comment"), class: "createcomment form-control" %>
    <div class="col-md-2">
      <%= f.submit t("post"), class: "btn btn-primary" %>
    </div>
  <% end %>
</div>
